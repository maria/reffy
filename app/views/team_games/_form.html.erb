<%= form_for(@team_game) do |f| %>
  <% if @team_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_game.errors.count, "error") %> prohibited this team_game from being saved:</h2>

      <ul>
      <% @team_game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team_id %><br />
    <%= f.number_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :game_id %><br />
    <%= f.number_field :game_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
