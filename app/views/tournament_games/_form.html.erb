<%= form_for(@tournament_game) do |f| %>
  <% if @tournament_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tournament_game.errors.count, "error") %> prohibited this tournament_game from being saved:</h2>

      <ul>
      <% @tournament_game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tournament_id %><br />
    <%= f.number_field :tournament_id %>
  </div>
  <div class="field">
    <%= f.label :game_id %><br />
    <%= f.number_field :game_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
